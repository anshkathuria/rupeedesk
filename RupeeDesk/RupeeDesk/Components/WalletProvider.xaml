<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:RupeeDesk.Utilities"
             x:Class="RupeeDesk.Components.WalletProvider">
  <StackLayout Orientation="Horizontal" Padding="10">
    <Label VerticalOptions="Center" FontSize="Large" HorizontalOptions="FillAndExpand"  Text="{Binding Name}" />
    <controls:FavoriteImage HeightRequest="40" WidthRequest="40" HorizontalOptions="EndAndExpand" Source="ic_check_box_outline" x:Name="IsTracked">
      <Image.GestureRecognizers>
        <TapGestureRecognizer Tapped="AddProvider"/>
      </Image.GestureRecognizers>
    </controls:FavoriteImage>
  </StackLayout>
</ContentView>